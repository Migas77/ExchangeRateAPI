# ci.yml
name: CI

on:
  push:
    branches: [main]

jobs:

  # test:
  #   uses: ./.github/workflows/test.yml

  release:
    # needs: test
    uses: ./.github/workflows/release.yml

  image-build-and-push:
    needs: release
    uses: ./.github/workflows/image-build-and-push.yml
    secrets: inherit
