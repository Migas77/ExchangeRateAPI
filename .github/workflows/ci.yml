name: CI

on:
  push:
    branches:
      - 'main'

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:

  # test:
  #   uses: ./.github/workflows/test.yml

  release:
    # needs: test
    uses: ./.github/workflows/release.yml

